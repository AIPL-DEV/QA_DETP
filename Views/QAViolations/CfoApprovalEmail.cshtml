@model long
@using Microsoft.Extensions.Configuration

@inject IConfiguration Configuration

@{
	Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Quality Non-Conformance Notification</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f6f8fa;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 600px;
            margin: 30px auto;
            background-color: #ffffff;
            padding: 30px;
            border: 1px solid #e1e4e8;
            border-radius: 6px;
        }

        .header {
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 20px;
            color: #333333;
        }

        .content {
            font-size: 16px;
            color: #555555;
            margin-bottom: 20px;
            line-height: 1.6;
        }

        .button {
            display: inline-block;
            padding: 12px 20px;
            background-color: #007BFF;
            color: #ffffff;
            text-decoration: none;
            border-radius: 4px;
            font-weight: bold;
        }

        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: #999999;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        @* <div class="header">Quality Non-Conformance Notification</div> *@
        <div class="content">
            Dear Sir/Ma'am,<br><br>
            The connsequnce for the recent quality non- conformance has ben finalized as per company norms. You are kindly requested to proceed with the applicable deduction process.
            Click the link to open.
        </div>
        <a href="@(Configuration["SiteDetails:Production_URL"])/QAViolations/action/@Model" class="button">Review Consequence</a>
        <div class="footer">
            @* ©    2025 Quality Management System *@
        </div>
    </div>
</body>
</html>
